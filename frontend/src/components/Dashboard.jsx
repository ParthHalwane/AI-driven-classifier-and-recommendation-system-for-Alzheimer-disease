import React, { useState } from "react";
import Form from "./Form";

function Dashboard() {
    return (
        <div>
            <h1>Dashboard</h1>
            <Form />
        </div>
    );
}

export default Dashboard;
